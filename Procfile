web: python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py shell -c "import os; from django.contrib.auth.models import User; u=os.getenv('DJANGO_SUPERUSER_USERNAME',''); e=os.getenv('DJANGO_SUPERUSER_EMAIL',''); p=os.getenv('DJANGO_SUPERUSER_PASSWORD',''); u=u.strip(); e=e.strip(); p=p.strip(); user=User.objects.filter(username=u).first() if u else None; user = User.objects.create_superuser(u,e,p) if (u and e and p and not user) else user; (setattr(user,'is_superuser',True), setattr(user,'is_staff',True), user.set_password(p), user.save()) if (user and p and (not user.is_superuser or not user.is_staff)) else None" && gunicorn Project.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
